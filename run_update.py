import asyncio; from sqlalchemy import select; from src.db.deps import get_db; from src.projects.models import Project; import src.companies.models; import src.users.models; async def main(): async for db in get_db(): res = await db.execute(select(Project).where(Project.id == 2)); p = res.scalars().first(); p.telegram_chat_id = '-1003909405708' if p else print('❌ Проект не найден'); await db.commit() if p else None; print('🔥 УСПЕХ! Chat ID привязан к проекту.') if p else None; break; asyncio.run(main())
