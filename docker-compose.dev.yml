volumes:
  # Используем отдельные тома для dev, чтобы не мешать prod данным
  postgres_data_dev:
  redis_data_dev:

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: pm-assistant-db-dev
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    env_file:
      - .env
    ports:
      - "5432:5432"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: pm-assistant-redis-dev
    volumes:
      - redis_data_dev:/data
    ports:
      - "6379:6379"
    restart: unless-stopped

  api:
    build:
      context: .
      target: runtime
    container_name: pm-assistant-api-dev
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - .:/app
      - /app/.venv  # Анонимный том, чтобы локальный .venv не затирал серверный
    # Переопределяем команду для hot-reload
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  worker:
    build:
      context: .
      target: runtime
    container_name: pm-assistant-worker-dev
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/.venv
    entrypoint: ["/app/worker.sh"]
    depends_on:
      - postgres
      - redis
    restart: unless-stopped
